# 智撰 PatentWriter — 核心功能实现规划

> **规划日期**：2026-05-27
> **核心目标**：实现【通过对话方式让 AI 辅助编写文档，实时引用 Dify 知识库内容，标注来源，保证文档的专业性和可溯源性】
> **当前状态**：后端和前端基础架构已搭建，需实现核心业务逻辑和 Dify 集成

---

## 一、核心功能实现任务分解

### 🎯 任务组 1：Dify 知识库集成（优先级 P0）

#### 任务 1.1：完善 Dify 客户端实现
**目标**：实现 `app/clients/dify_client.py` 的完整功能

**具体步骤**：
1. ✅ 实现 `DifyClient.retrieve()` 方法
   - 调用 Dify 知识库检索 API：`POST /datasets/{dataset_id}/retrieve`
   - 参数：query, knowledge_id, top_k, score_threshold, rerank
   - 超时控制（3s）+ 重试机制（1次）
   - 结果缓存（相同 query + knowledge_id）

2. ✅ 实现 `DifyClient.check_health()` 方法
   - 健康检查：验证 Dify 服务连通性
   - 返回：服务状态、延迟、错误信息

3. ✅ 实现 `FallbackManager` 降级策略
   - 主 Dify 实例 → 备用 Dify 实例 → 本地知识库 → 无知识库
   - 每种策略的实现和切换逻辑

**验收标准**：
- ✅ 能成功调用 Dify API 并获取检索结果
- ✅ 超时和失败时能正确触发降级
- ✅ 缓存机制正常工作

**预计时间**：4-6 小时

---

#### 任务 1.2：完善知识库配置管理
**目标**：实现知识库配置 CRUD 和测试连接功能

**具体步骤**：
1. ✅ 完善 `app/api/knowledge.py` API 路由
   - `POST /api/v1/knowledge/configs` - 新增配置
   - `GET /api/v1/knowledge/configs` - 配置列表
   - `PUT /api/v1/knowledge/configs/{id}` - 更新配置
   - `DELETE /api/v1/knowledge/configs/{id}` - 删除配置
   - `POST /api/v1/knowledge/configs/{id}/test` - 测试连接

2. ✅ 实现 `app/services/knowledge_svc.py` 业务逻辑
   - 配置的增删改查
   - 测试连接：调用 `DifyClient.check_health()`
   - 设置默认知识库

3. ✅ 数据库迁移：创建 `knowledge_configs` 表
   - 使用 Alembic 创建表结构
   - 插入默认配置（可选）

**验收标准**：
- ✅ 能通过 API 管理知识库配置
- ✅ 测试连接功能正常工作
- ✅ 数据库表结构正确

**预计时间**：3-4 小时

---

### 🎯 任务组 2：对话式文档生成（优先级 P0）

#### 任务 2.1：实现对话管理与消息发送
**目标**：实现对话创建、消息发送、流式响应

**具体步骤**：
1. ✅ 完善 `app/api/conversations.py` API 路由
   - `POST /api/v1/conversations` - 创建对话
   - `GET /api/v1/conversations` - 对话列表（分页、搜索）
   - `GET /api/v1/conversations/{id}` - 对话详情（含消息）
   - `PUT /api/v1/conversations/{id}` - 更新对话标题
   - `DELETE /api/v1/conversations/{id}` - 删除对话

2. ✅ 实现 `POST /api/v1/conversations/{id}/messages` 核心接口
   - 接收用户消息
   - 调用 `DifyClient.retrieve()` 检索知识库
   - 调用 `LLMService.generate()` 生成文档
   - 解析引用标注
   - 持久化到数据库
   - 返回生成的文档和引用列表

3. ✅ 实现 SSE 流式接口 `GET /api/v1/conversations/{id}/stream`
   - 推送消息开始事件
   - 推送引用事件
   - 推送内容增量事件
   - 推送引用标注事件
   - 推送文档生成完成事件
   - 推送完成事件

**验收标准**：
- ✅ 能创建对话并发送消息
- ✅ 能接收流式响应并实时渲染
- ✅ 消息和对话数据正确持久化

**预计时间**：6-8 小时

---

#### 任务 2.2：实现 LLM 调用封装
**目标**：实现 `app/services/llm_svc.py` 统一 LLM 调用入口

**具体步骤**：
1. ✅ 实现 `LLMService.generate()` 方法
   - 支持流式输出（AsyncGenerator）
   - 构建 system_prompt（根据任务类型）
   - 注入 references（Dify 检索结果）
   - 传入对话历史（messages）
   - 调用 Dify 的 `/chat-messages` API 或外部 LLM API

2. ✅ 实现 `LLMService.build_system_prompt()` 方法
   - 根据任务类型（权利要求书/说明书/技术交底书）构建 Prompt
   - 包含引用格式要求
   - 包含输出格式要求

3. ✅ Prompt 模板管理（`app/core/prompt_templates.py`）
   - 定义不同类型的 Prompt 模板
   - 支持变量替换（如 {references}, {history}, {user_message}）

**验收标准**：
- ✅ 能成功调用 LLM 生成文档内容
- ✅ 流式输出正常工作
- ✅ Prompt 模板正确注入

**预计时间**：4-6 小时

---

#### 任务 2.3：实现引用解析引擎
**目标**：实现 `app/core/citation_parser.py` 从 LLM 输出中解析引用标注

**具体步骤**：
1. ✅ 实现 `CitationParser.parse()` 方法
   - 正则匹配所有 [①]-[⑨] 标记
   - 计算标记在文档中的字符偏移量
   - 与 Dify 返回的检索结果关联
   - 输出 Citation[] 数组

2. ✅ 实现 `CitationParser.reparse_after_edit()` 方法
   - 用户编辑文档后重新解析
   - 同步更新引用位置

3. ✅ 引用标注格式约定
   - LLM 输出格式规范
   - 示例和 Few-shot 示例

**验收标准**：
- ✅ 能正确解析引用标注
- ✅ 引用位置计算准确
- ✅ 编辑后引用同步更新

**预计时间**：3-4 小时

---

### 🎯 任务组 3：文档管理与导出（优先级 P0）

#### 任务 3.1：实现文档管理 API
**目标**：实现文档的 CRUD 和导出功能

**具体步骤**：
1. ✅ 完善 `app/api/documents.py` API 路由
   - `GET /api/v1/documents/{id}` - 获取文档详情（含引用）
   - `PUT /api/v1/documents/{id}` - 更新文档内容
   - `POST /api/v1/documents/{id}/export` - 导出为 Word

2. ✅ 实现 `app/services/document_svc.py` 业务逻辑
   - 文档创建（关联对话）
   - 文档更新（版本控制）
   - 文档查询

3. ✅ 实现 `app/services/export_svc.py` Word 导出
   - 使用 python-docx 生成 .docx 文件
   - HTML → Word 格式转换
   - 引用标注保留（超链接或脚注）
   - 附加引用来源列表作为附录

**验收标准**：
- ✅ 能创建和更新文档
- ✅ Word 导出功能正常
- ✅ 引用标注在导出文档中可见

**预计时间**：4-6 小时

---

#### 任务 3.2：实现引用管理 API
**目标**：实现引用列表查询和管理

**具体步骤**：
1. ✅ 完善 `app/api/citations.py` API 路由
   - `GET /api/v1/documents/{id}/citations` - 获取文档引用列表
   - `GET /api/v1/citations/{id}` - 引用详情（含原始片段）

2. ✅ 实现 `app/services/citation_svc.py` 业务逻辑
   - 引用列表查询（按引用次数倒序）
   - 引用详情查询
   - 引用同步更新（文档编辑后）

**验收标准**：
- ✅ 能正确查询引用列表
- ✅ 引用详情完整
- ✅ 编辑后引用同步更新

**预计时间**：2-3 小时

---

### 🎯 任务组 4：前端三栏布局与核心交互（优先级 P0）

#### 任务 4.1：实现三栏布局
**目标**：实现左侧对话列表、中间聊天/文档界面、右侧引用列表的三栏布局

**具体步骤**：
1. ✅ 实现 `AppLayout.tsx` 三栏布局容器
   - 左侧边栏：260px（对话列表）
   - 主内容区：自适应
   - 右侧边栏：300px（引用列表）
   - 响应式适配（≥1440px 完整三栏，<1440px 部分收起）

2. ✅ 实现 `Sidebar.tsx` 左侧对话列表
   - 对话列表展示（按时间倒序）
   - 搜索框
   - 新建对话按钮
   - 对话项点击切换
   - 鼠标悬停显示更多操作（重命名、删除）

3. ✅ 实现 `CitationPanel.tsx` 右侧引用列表
   - 引用来源列表（按引用次数倒序）
   - 引用项展开/收起
   - 片段预览
   - 点击跳转到正文对应位置

**验收标准**：
- ✅ 三栏布局正确渲染
- ✅ 响应式适配正常工作
- ✅ 左右侧边栏交互正常

**预计时间**：4-6 小时

---

#### 任务 4.2：实现对话式聊天界面
**目标**：实现 Chat 对话界面，支持消息展示和输入框

**具体步骤**：
1. ✅ 实现 `ChatView.tsx` 对话主视图
   - 消息气泡（用户消息右侧深色，AI 消息左侧浅色）
   - 文档卡片展示
   - 引用标注显示

2. ✅ 实现 `MessageBubble.tsx` 消息气泡
   - 支持用户消息和 AI 消息不同样式
   - 支持文档卡片渲染

3. ✅ 实现 `DocumentCard.tsx` 文档卡片
   - 文档标题
   - 文档预览
   - 引用计数
   - 操作按钮（查看完整文档、插入编辑器、下载）

4. ✅ 实现 `ChatInput.tsx` 输入框
   - 多行输入
   - 发送按钮
   - 快捷指令（可选）
   - 附件上传（可选）

5. ✅ 实现 `StreamingMessage.tsx` 流式消息渲染
   - SSE 流式接收
   - 实时渲染内容增量
   - 引用标注实时更新

**验收标准**：
- ✅ 聊天界面正确渲染
- ✅ 消息发送和接收正常
- ✅ 流式输出正常工作

**预计时间**：6-8 小时

---

#### 任务 4.3：实现文档详情与编辑界面
**目标**：实现文档阅读/编辑视图，支持引用标注高亮

**具体步骤**：
1. ✅ 实现 `DocumentView.tsx` 文档阅读视图
   - 富文本渲染
   - 引用标注高亮
   - 点击引用标注跳转到右侧对应来源

2. ✅ 实现 `DocumentEditor.tsx` 文档编辑视图
   - 集成 TipTap 编辑器
   - 支持格式：标题、段落、列表、加粗、斜体等
   - 支持直接拖拽 AI 生成的内容到指定位置

3. ✅ 实现 `CitationMark.tsx` 引用标注组件
   - 上标样式
   - 可点击跳转
   - 高亮显示

4. ✅ 实现 `TiptapExtensions.ts` TipTap 自定义扩展
   - CitationMark（引用标注 Mark）
   - PatentSection（专利文档章节 Node）

**验收标准**：
- ✅ 文档阅读视图正确渲染
- ✅ 文档编辑视图正常工作
- ✅ 引用标注正确显示和交互

**预计时间**：6-8 小时

---

#### 任务 4.4：实现状态管理与 API 调用
**目标**：实现前端状态管理和后端 API 调用

**具体步骤**：
1. ✅ 实现 `conversationStore.ts` 对话状态管理
   - 对话列表
   - 当前对话
   - 消息列表
   - 创建对话、发送消息、删除对话等方法

2. ✅ 实现 `documentStore.ts` 文档状态管理
   - 当前文档
   - 文档更新
   - 文档导出

3. ✅ 实现 `citationStore.ts` 引用状态管理
   - 引用列表
   - 当前高亮引用
   - 引用同步更新

4. ✅ 实现 `knowledgeStore.ts` 知识库配置状态管理
   - 知识库配置列表
   - 当前选中的知识库

5. ✅ 实现 API 调用服务（`services/api.ts`, `services/conversationApi.ts`, 等）
   - 封装后端 API 调用
   - 错误处理
   - SSE 流式接收

6. ✅ 实现 `useSSE.ts` SSE 流式接收 Hook
   - 建立 SSE 连接
   - 解析事件流
   - 推送数据到状态管理

**验收标准**：
- ✅ 状态管理正常工作
- ✅ API 调用成功
- ✅ SSE 流式接收正常

**预计时间**：4-6 小时

---

## 二、需要在 Dify 上实现的 Agent 功能梳理

### 🤖 Agent 功能清单

基于你的 PRD 和技术架构，以下是需要在 Dify 上实现或配置的 Agent 功能：

---

#### Agent 1：知识库检索 Agent（核心，必须）
**功能**：根据用户查询，从知识库中检索相关片段

**实现方式**：
- ✅ 使用 Dify 自带的**知识库检索**功能
- ✅ 配置知识库：上传文档、分块、Embedding
- ✅ 配置检索参数：Top-K、相似度阈值、重排序

**Dify 配置步骤**：
1. 登录 Dify 控制台
2. 创建知识库
3. 上传专利文档（PDF、Word、TXT 等）
4. 配置分块策略（建议：段落级或句子级）
5. 配置 Embedding 模型（使用 Dify 内置或外部模型）
6. 配置检索参数：
   - Top-K：5（可调整）
   - 相似度阈值：0.7（可调整）
   - 重排序：开启
7. 测试检索效果

**API 调用**：
- 后端调用 `POST /datasets/{dataset_id}/retrieve` API
- 返回：片段内容、来源文档名称、相似度分数、片段位置

**优先级**：P0（核心功能，必须实现）

---

#### Agent 2：对话式文档生成 Agent（核心，必须）
**功能**：根据用户指令和检索到的知识库内容，生成专利文档

**实现方式**：
- ✅ 使用 Dify 的**对话型应用**功能
- ✅ 配置 Prompt 模板
- ✅ 配置上下文注入（知识库检索结果）
- ✅ 配置引用标注格式要求

**Dify 配置步骤**：
1. 登录 Dify 控制台
2. 创建对话型应用
3. 配置 Prompt 模板：
   ```
   你是一个专业的专利文档撰写助手。
   
   用户需求：{user_message}
   
   相关知识库内容：
   {references}
   
   对话历史：
   {history}
   
   请根据以上信息，生成一份专业的专利文档。
   
   要求：
   1. 使用专业的专利文档语言风格
   2. 引用知识库内容时，在引用位置标注 [①][②][③] 等标记
   3. 在文档末尾列出所有引用来源
   4. 确保文档结构清晰（技术领域、背景技术、发明内容等）
   
   输出格式：
   [①本发明涉及一种...]
   
   二、背景技术
   [②现有技术中...]
   
   ...
   
   【引用来源】
   [1] 专利文档A.pdf - "本发明涉及..."
   [2] 技术交底书.pdf - "现有技术中..."
   ```

4. 配置上下文变量：
   - `user_message`：用户消息
   - `references`：知识库检索结果
   - `history`：对话历史

5. 配置模型：
   - 使用 Dify 内置模型或外部模型（Claude/GPT/国产模型）

6. 测试对话效果

**API 调用**：
- 后端调用 `POST /chat-messages` API
- 传入：query, inputs (references, history), response_mode (streaming)
- 返回：流式生成的文档内容

**优先级**：P0（核心功能，必须实现）

---

#### Agent 3：多轮对话编辑 Agent（核心，必须）
**功能**：在已有文档基础上，根据用户指令继续修改文档

**实现方式**：
- ✅ 复用 Agent 2 的对话型应用
- ✅ 在 Prompt 中注入已有文档内容
- ✅ 保持对话历史，实现多轮对话

**Dify 配置步骤**：
1. 在 Agent 2 的基础上，修改 Prompt 模板：
   ```
   你是一个专业的专利文档撰写助手。
   
   已有文档内容：
   {existing_document}
   
   用户修改指令：{user_message}
   
   相关知识库内容：
   {references}
   
   对话历史：
   {history}
   
   请根据用户指令，在已有文档基础上进行修改。
   
   要求：
   1. 保持已有文档的风格和结构
   2. 根据指令修改对应部分
   3. 引用新知识库内容时，标注新的引用标记
   4. 更新引用来源列表
   
   输出格式：
   修改后的完整文档内容...
   ```

2. 配置上下文变量：
   - `existing_document`：已有文档内容
   - 其他变量同 Agent 2

3. 测试多轮对话效果

**API 调用**：
- 同 Agent 2，但传入 `existing_document` 变量

**优先级**：P0（核心功能，必须实现）

---

#### Agent 4：联网查询 Agent（可选，P1）
**功能**：当 Dify 知识库无相关内容时，自动联网查询相关信息

**实现方式**：
- ✅ 使用 Dify 的**工作流**功能
- ✅ 配置条件分支：知识库检索结果为空 → 联网查询
- ✅ 集成联网搜索 API（如 SerpAPI、Bing Search API 等）

**Dify 配置步骤**：
1. 登录 Dify 控制台
2. 创建工作流应用
3. 配置节点：
   - 开始节点：接收用户查询
   - 知识库检索节点：调用知识库检索
   - 条件分支节点：判断检索结果是否为空
   - 联网查询节点（条件为真时执行）：调用联网搜索 API
   - 文档生成节点：根据检索结果或联网结果生成文档
   - 结束节点：返回生成的文档

4. 配置联网搜索 API：
   - 申请 API Key（如 SerpAPI）
   - 在 Dify 中配置 HTTP 请求节点

5. 测试工作流

**API 调用**：
- 后端调用工作流的 API 端点
- 传入：query
- 返回：生成的文档内容（可能包含联网查询结果）

**注意**：
- 联网查询结果**不标注引用**（PRD 要求）
- 需要在前端提示用户"当前内容来自联网查询，未标注引用"

**优先级**：P1（可选功能，MVP 可不包含）

---

#### Agent 5：文档结构导航 Agent（可选，P1）
**功能**：自动识别文档结构，生成文档目录和导航

**实现方式**：
- ✅ 使用 Dify 的**对话型应用**或**工作流**
- ✅ 传入文档内容，让 LLM 识别章节结构
- ✅ 返回文档目录 JSON

**Dify 配置步骤**：
1. 创建对话型应用或工作流
2. 配置 Prompt：
   ```
   请分析以下专利文档的结构，识别出所有章节标题和层级关系。
   
   文档内容：
   {document_content}
   
   请返回 JSON 格式的目录：
   {
     "sections": [
       {"level": 1, "title": "一、技术领域", "position": 0},
       {"level": 1, "title": "二、背景技术", "position": 150},
       ...
     ]
   }
   ```

3. 测试效果

**API 调用**：
- 后端调用 API，传入文档内容
- 返回：文档目录 JSON

**前端**：
- 根据目录 JSON 生成文档结构导航
- 点击导航项跳转到对应位置

**优先级**：P1（可选功能，MVP 可不包含）

---

#### Agent 6：引用质量评估 Agent（可选，P1）
**功能**：评估引用标注的准确性，检测张冠李戴的情况

**实现方式**：
- ✅ 使用 Dify 的**对话型应用**或**工作流**
- ✅ 传入文档内容和引用标注，让 LLM 评估准确性
- ✅ 返回评估结果和建议

**Dify 配置步骤**：
1. 创建对话型应用或工作流
2. 配置 Prompt：
   ```
   请评估以下专利文档的引用标注准确性。
   
   文档内容：
   {document_content}
   
   引用标注：
   {citations}
   
   请评估每个引用标注是否正确，即标注位置的内容是否确实来自标注的来源。
   
   请返回 JSON 格式的评估结果：
   {
     "evaluations": [
       {"citation_id": "1", "accuracy": 0.95, "is_correct": true, "reason": "..."},
       ...
     ]
   }
   ```

3. 测试效果

**API 调用**：
- 后端调用 API，传入文档内容和引用标注
- 返回：评估结果 JSON

**前端**：
- 根据评估结果，高亮显示可能有问题的引用标注
- 提示用户人工审核

**优先级**：P1（可选功能，MVP 可不包含）

---

### 📋 Dify Agent 功能总结表

| Agent 功能 | 实现方式 | 优先级 | 预计配置时间 | 是否需流程编排 |
|-----------|---------|--------|--------------|--------------|
| **知识库检索** | Dify 知识库功能 | P0 | 2-3 小时 | 否 |
| **对话式文档生成** | Dify 对话型应用 | P0 | 3-4 小时 | 否 |
| **多轮对话编辑** | 复用对话型应用 | P0 | 1-2 小时 | 否 |
| **联网查询** | Dify 工作流 | P1 | 4-6 小时 | ✅ 是 |
| **文档结构导航** | Dify 对话型应用 | P1 | 2-3 小时 | 否 |
| **引用质量评估** | Dify 对话型应用/工作流 | P1 | 3-4 小时 | 可选 |

---

## 三、明天重点工作建议

### 🎯 今天（2026-05-27）可以完成：
1. ✅ **任务 1.1**：完善 Dify 客户端实现（4-6 小时）
2. ✅ **任务 1.2**：完善知识库配置管理（3-4 小时）

**目标**：完成 Dify 知识库集成，能成功调用 Dify API 检索知识库

---

### 🎯 明天（2026-05-28）重点工作：

#### 上午（9:00-12:00）：
1. ✅ **在 Dify 上配置知识库**（2-3 小时）
   - 登录 Dify 控制台
   - 创建知识库
   - 上传专利文档
   - 配置分块策略和检索参数
   - 测试检索效果

2. ✅ **在 Dify 上配置对话式文档生成 Agent**（3-4 小时）
   - 创建对话型应用
   - 配置 Prompt 模板
   - 配置上下文变量
   - 测试对话效果

#### 下午（14:00-18:00）：
3. ✅ **实现后端对话管理和消息发送**（任务 2.1，6-8 小时）
   - 实现消息发送 API
   - 集成 Dify 知识库检索和文档生成
   - 实现 SSE 流式接口

4. ✅ **实现引用解析引擎**（任务 2.3，3-4 小时）
   - 实现 CitationParser.parse()
   - 测试引用解析准确性

#### 晚上（可选，如果进度快）：
5. ✅ **开始实现前端三栏布局和聊天界面**（任务 4.1 和 4.2）
   - 实现 AppLayout.tsx
   - 实现 ChatView.tsx

---

## 四、项目文件结构检查与清理（已完成）

✅ **已完成**：
1. 检查项目冗余文件
2. 清理 Python 缓存文件（`__pycache__`）
3. 确认 `.gitignore` 配置完整
4. 创建项目清理报告

✅ **项目状态**：
- 后端基础架构已完成
- 前端基础架构已完成
- 冗余文件已清理
- 可以开始实现核心功能

---

## 五、三重身份视角审视

### 🔵 产品经理视角：
✅ **需求明确**：核心功能（对话式文档生成 + 引用标注）定义清晰
✅ **MVP 范围合理**：P0 功能可支撑最小可用版本
⚠️ **需注意**：Dify 配置可能需要多次迭代调试 Prompt，建议预留时间

### 🟡 测试视角：
✅ **测试点清晰**：Dify API 调用、引用解析、流式响应等都有明确验收标准
⚠️ **需注意**：
- Dify 服务不可用时的降级策略需要充分测试
- 引用标注准确性需要建立验证机制
- SSE 断线重连需要测试

### 🟢 开发视角：
✅ **技术栈成熟**：FastAPI + React + TipTap 都是生产级组件
✅ **架构清晰**：分层解耦，Dify 通过 Client 层解耦
⚠️ **需注意**：
- TipTap 自定义扩展（CitationMark）需要深度 ProseMirror 知识
- Prompt 工程是核心难点，需要迭代调试
- Dify API 调用需要注意超时和重试

---

## 六、下一步行动清单

### ✅ 今天完成（2026-05-27）：
- [ ] 完善 Dify 客户端实现（任务 1.1）
- [ ] 完善知识库配置管理（任务 1.2）
- [ ] 测试 Dify API 调用

### ✅ 明天完成（2026-05-28）：
- [ ] 在 Dify 上配置知识库
- [ ] 在 Dify 上配置对话式文档生成 Agent
- [ ] 实现后端对话管理和消息发送（任务 2.1）
- [ ] 实现引用解析引擎（任务 2.3）

### ✅ 本周完成（2026-05-29 - 2026-05-30）：
- [ ] 实现 LLM 调用封装（任务 2.2）
- [ ] 实现文档管理与导出（任务 3.1 和 3.2）
- [ ] 实现前端三栏布局和核心交互（任务 4.1 - 4.4）
- [ ] 集成测试

---

**规划完成时间**：2026-05-27 20:30 GMT+8
**下一步**：开始执行任务 1.1 和 1.2，完善 Dify 客户端和知识库配置管理