# Dify 对话式文档生成 Agent 实现路径

> **目标**：在 Dify 中配置【对话式文档生成 Agent】，实现根据用户指令和检索到的知识库内容生成专利文档
> **预计总时间**：6-8 小时（分 6 个可验证步骤）
> **输出**：可独立运行的 Dify Agent + API 调用测试脚本

---

## 前置准备（30 分钟）

### 1. 确认 Dify 服务可用

**操作**：
1. 确认 Dify 服务已启动（本地或远程）
2. 打开浏览器访问 Dify 控制台（`http://localhost:5001` 或你的 Dify 地址）
3. 登录 Dify 账号

**验收标准**：
- ✅ 能成功登录 Dify 控制台
- ✅ 能看到 Dify 主界面（仪表盘）

**预计时间**：10 分钟

---

### 2. 准备测试用的专利文档

**操作**：
1. 准备 2-3 个测试用的专利相关文档（PDF、Word、TXT 等格式）
   - 示例：一份专利文档 PDF、一份技术交底书 Word、一份现有技术说明 TXT
2. 确保文档内容包含中文，且有一定的技术描述
3. 将文档放在一个临时目录中，方便上传

**验收标准**：
- ✅ 有至少 2 个测试文档
- ✅ 文档格式是 Dify 支持的（PDF、Word、TXT、Markdown 等）
- ✅ 文档内容包含中文

**预计时间**：20 分钟

---

## 步骤 1：Dify 知识库配置（2-3 小时）

### 目标
在 Dify 中创建知识库，上传测试文档，配置检索参数，并验证检索效果。

---

### 1.1 创建知识库

**操作**：
1. 在 Dify 控制台，点击左侧菜单 **"知识库"**
2. 点击 **"创建知识库"** 按钮
3. 填写知识库信息：
   - **知识库名称**：`PatentWriter-Test`（测试用）
   - **描述**：`用于测试专利文档生成的知识库`
4. 点击 **"创建"**

**验收标准**：
- ✅ 知识库创建成功
- ✅ 能看到知识库详情页面
- ✅ 记录 **知识库 ID**（后续 API 调用需要）

**预计时间**：10 分钟

---

### 1.2 上传文档并配置分块策略

**操作**：
1. 在知识库详情页面，点击 **"添加文档"**
2. 上传你准备的测试文档（PDF、Word 等）
3. 配置分块策略：
   - **分块方式**：选择 **"自动分块"**（推荐）或 **"按段落分块"**
   - **分块大小**：默认 500 字符（可根据文档调整）
   - **分块重叠**：默认 50 字符（保持上下文连贯）
   - **文本预处理**：开启 **"清理多余空格和换行"**
4. 点击 **"确认"** 开始处理文档
5. 等待文档处理完成（状态变为 **"可用"**）

**验收标准**：
- ✅ 文档上传成功
- ✅ 文档状态为 **"可用"**（不是"处理中"或"失败"）
- ✅ 能查看文档的分块预览（点击文档名称 → 查看分块）

**预计时间**：30-60 分钟（取决于文档大小和处理速度）

---

### 1.3 配置检索参数

**操作**：
1. 在知识库详情页面，点击 **"检索设置"** 标签
2. 配置检索参数：
   - **Top-K**：`5`（每次检索返回 5 个最相关的片段）
   - **相似度阈值**：`0.7`（低于此分数的片段会被过滤）
   - **重排序**：开启（提高检索准确性）
   - **检索模式**：选择 **"语义检索"**（基于 Embedding 的语义匹配）
3. 点击 **"保存"**

**验收标准**：
- ✅ 检索参数保存成功
- ✅ 能查看到配置的参数值

**预计时间**：10 分钟

---

### 1.4 测试知识库检索效果

**操作**：
1. 在知识库详情页面，点击 **"检索测试"** 标签
2. 输入测试查询：`深度学习图像识别专利`
3. 点击 **"检索"**
4. 查看检索结果：
   - 是否返回了相关片段？
   - 片段内容是否来自你上传的文档？
   - 相似度分数是否 ≥ 0.7？
5. 尝试不同的查询，观察检索效果

**验收标准**：
- ✅ 检索能返回结果（不是空结果）
- ✅ 返回的结果片段确实来自上传的文档
- ✅ 相似度分数 ≥ 0.7（或接近）
- ✅ 检索响应时间 < 3 秒

**预计时间**：30 分钟

---

### 1.5 记录知识库配置参数

**操作**：
创建一个配置文件（后续后端开发需要），记录以下信息：

```json
{
  "dify_base_url": "http://localhost:5001",  // 你的 Dify 服务地址
  "dify_api_key": "YOUR_API_KEY",           // 后续创建应用时生成
  "knowledge_id": "YOUR_KNOWLEDGE_ID",      // 知识库 ID（步骤 1.1 记录）
  "top_k": 5,
  "score_threshold": 0.7,
  "rerank_enabled": true
}
```

**验收标准**：
- ✅ 配置文件已创建
- ✅ 所有参数已填写（API Key 暂时留空，后续步骤生成）

**预计时间**：10 分钟

---

## 步骤 2：创建对话型应用并配置 Prompt（1-2 小时）

### 目标
在 Dify 中创建对话型应用，配置 Prompt 模板，并测试对话效果。

---

### 2.1 创建对话型应用

**操作**：
1. 在 Dify 控制台，点击左侧菜单 **"应用"**
2. 点击 **"创建应用"**
3. 选择 **"对话型应用"**
4. 填写应用信息：
   - **应用名称**：`PatentWriter-Agent`
   - **描述**：`专利文档生成 Agent`
   - **图标**：选择一个合适的图标
5. 点击 **"创建"**

**验收标准**：
- ✅ 应用创建成功
- ✅ 能看到应用详情页面（Prompt 配置界面）
- ✅ 记录 **应用 ID**（后续 API 调用需要）

**预计时间**：10 分钟

---

### 2.2 配置 Prompt 模板

**操作**：
1. 在应用详情页面，找到 **"Prompt"** 配置区域
2. 输入以下 Prompt 模板：

```
你是一个专业的专利文档撰写助手。

用户需求：{{#user_message#}}

相关知识库内容：
{{#references#}}

对话历史：
{{#history#}}

请根据以上信息，生成一份专业的专利文档。

要求：
1. 使用专业的专利文档语言风格
2. 引用知识库内容时，在引用位置标注 [①][②][③] 等标记
3. 在文档末尾列出所有引用来源
4. 确保文档结构清晰（技术领域、背景技术、发明内容等）

输出格式：
[①本发明涉及一种...]

二、背景技术
[②现有技术中...]

...

【引用来源】
[1] 专利文档A.pdf - "本发明涉及..."
[2] 技术交底书.pdf - "现有技术中..."
```

3. 点击 **"保存"**

**验收标准**：
- ✅ Prompt 模板保存成功
- ✅ 模板中包含变量 `{{#user_message#}}`、`{{#references#}}`、`{{#history#}}`

**预计时间**：20 分钟

---

### 2.3 配置上下文变量

**操作**：
1. 在应用详情页面，找到 **"上下文变量"** 配置区域
2. 添加以下变量：
   - **变量名**：`user_message`（用户消息）
   - **变量名**：`references`（知识库检索结果）
   - **变量名**：`history`（对话历史）
3. 对于每个变量，配置：
   - **类型**：`String`（字符串）
   - **必填**：`No`（否）
   - **默认值**：空（或示例值）
4. 点击 **"保存"**

**验收标准**：
- ✅ 上下文变量保存成功
- ✅ 变量列表中能看到 `user_message`、`references`、`history`

**预计时间**：20 分钟

---

### 2.4 配置模型

**操作**：
1. 在应用详情页面，找到 **"模型"** 配置区域
2. 选择模型：
   - 如果使用 Dify 内置模型，直接选择（如 `gpt-3.5-turbo`、`claude-3` 等）
   - 如果使用外部模型，需要先配置模型供应商（设置 → 模型供应商）
3. 配置模型参数：
   - **温度**：`0.7`（适中，既不会太死板也不会太随机）
   - **最大生成长度**：`4096`（根据专利文档长度调整）
4. 点击 **"保存"**

**验收标准**：
- ✅ 模型配置保存成功
- ✅ 能选择模型（不是灰色不可用状态）

**预计时间**：20 分钟

---

### 2.5 测试对话效果（Dify UI）

**操作**：
1. 在应用详情页面，点击右上角 **"预览"** 按钮
2. 在预览窗口中，输入测试消息：
   ```
   帮我写一份关于深度学习图像识别的专利交底书
   ```
3. 观察 AI 回复：
   - 是否生成了专利文档内容？
   - 是否标注了引用 `[①][②]`？
   - 引用来源是否正确列出？
4. 尝试多轮对话：
   - 输入：`修改第三段，加入更多技术细节`
   - 观察 AI 是否基于已有文档进行修改

**验收标准**：
- ✅ AI 能生成专利文档内容
- ✅ 引用标注 `[①][②]` 出现（如果知识库检索到了相关内容）
- ✅ 多轮对话能保持上下文

**预计时间**：30 分钟

---

## 步骤 3：配置知识库检索集成（1 小时）

### 目标
在 Dify 应用中配置知识库检索，使 Agent 能自动调用知识库检索并注入到 Prompt 中。

---

### 3.1 启用知识库检索

**操作**：
1. 在应用详情页面，找到 **"上下文"** 配置区域
2. 点击 **"添加知识库"**
3. 选择你在 **步骤 1** 中创建的知识库（`PatentWriter-Test`）
4. 配置检索参数（可以与知识库设置不同）：
   - **Top-K**：`5`
   - **相似度阈值**：`0.7`
   - **重排序**：开启
5. 点击 **"保存"**

**验收标准**：
- ✅ 知识库已添加到应用的上下文中
- ✅ 能看到知识库名称和配置参数

**预计时间**：20 分钟

---

### 3.2 测试知识库检索集成

**操作**：
1. 在应用预览窗口中，输入测试消息：
   ```
   帮我写一份关于深度学习图像识别的专利交底书
   ```
2. 观察 AI 回复：
   - 是否引用了知识库中的内容？
   - 引用标注 `[①][②]` 是否正确？
   - 引用来源是否列出？
3. 检查 Dify 日志（如果有）：
   - 是否调用了知识库检索 API？
   - 检索结果是否正确注入到 Prompt 中？

**验收标准**：
- ✅ AI 回复中引用了知识库内容
- ✅ 引用标注格式正确 `[①][②]`
- ✅ 引用来源列表正确

**预计时间**：30 分钟

---

## 步骤 4：测试引用标注格式（1 小时）

### 目标
验证 LLM 生成的引用标注格式是否符合预期，并能正确解析。

---

### 4.1 迭代优化 Prompt

**操作**：
1. 根据测试结果，调整 Prompt 模板（步骤 2.2）
2. 常见问题及优化建议：
   - **问题**：引用标注没有出现
     - **优化**：在 Prompt 中加强引用要求，例如：
       ```
       重要：你必须引用知识库内容。在引用位置标注 [①][②][③] 等标记。
       如果不引用，用户将无法追溯信息来源。
       ```
   - **问题**：引用标注格式不正确（如 `[1]` 而不是 `[①]`）
     - **优化**：在 Prompt 中给出 Few-shot 示例：
       ```
       示例输出格式：
       [①本发明涉及一种基于深度学习的图像识别方法，具体包括...]
       
       二、背景技术
       [②现有技术中，图像识别方法存在以下问题...]
       
       ...
       
       【引用来源】
       [1] 专利文档A.pdf - "本发明涉及一种基于..."
       [2] 技术交底书.pdf - "现有技术中..."
       ```
   - **问题**：引用来源列表缺失或不完整
     - **优化**：在 Prompt 中明确要求：
       ```
       在文档末尾，必须列出所有引用来源，格式为：
       【引用来源】
       [1] 文档名称 - "引用片段内容"
       [2] 文档名称 - "引用片段内容"
       ...
       ```
3. 每次修改 Prompt 后，都要测试对话效果

**验收标准**：
- ✅ 引用标注 `[①][②]` 稳定出现
- ✅ 引用标注位置正确（在引用内容旁边）
- ✅ 引用来源列表完整

**预计时间**：40 分钟

---

### 4.2 测试边界情况

**操作**：
1. 测试知识库无相关结果的情况：
   - 输入一个与知识库无关的查询（如 `帮我写一份关于量子计算的专利`）
   - 观察 AI 是否生成了内容，且**没有**引用标注（或标注为 `[无]`）
2. 测试多轮对话中的引用：
   - 第一轮：生成文档
   - 第二轮：修改某一段落
   - 观察引用标注是否更新（新的引用标记是否添加）
3. 测试长文档生成：
   - 输入一个需要生成长文档的查询
   - 观察是否生成完整，且引用标注贯穿全文

**验收标准**：
- ✅ 知识库无结果时，AI 能生成内容且不标注引用（或明确说明无引用）
- ✅ 多轮对话中引用标注能正确更新
- ✅ 长文档生成完整，引用标注无遗漏

**预计时间**：20 分钟

---

## 步骤 5：获取 API 凭证并编写测试脚本（1 小时）

### 目标
获取 Dify 应用的 API Key，编写 Python 脚本测试 API 调用，为明天后端开发做准备。

---

### 5.1 获取 API Key

**操作**：
1. 在应用详情页面，点击右上角 **"发布"** 按钮
2. 发布成功后，点击 **"API 访问"**
3. 在 API 访问页面，点击 **"创建 API Key"**
4. 复制生成的 API Key（格式类似 `dataset-xxx` 或 `app-xxx`）
5. 记录 **API 端点**：
   - 对话型应用的 API 端点：`POST /chat-messages`
   - 完整 URL：`http://your-dify-url/v1/chat-messages`

**验收标准**：
- ✅ 已创建 API Key
- ✅ 已记录 API Key 和 API 端点

**预计时间**：10 分钟

---

### 5.2 更新配置文件

**操作**：
更新你在 **步骤 1.5** 中创建的配置文件：

```json
{
  "dify_base_url": "http://localhost:5001",
  "dify_api_key": "YOUR_API_KEY_HERE",     // 填入步骤 5.1 获取的 API Key
  "knowledge_id": "YOUR_KNOWLEDGE_ID",
  "app_id": "YOUR_APP_ID",                 // 应用 ID
  "api_endpoint": "/v1/chat-messages",
  "top_k": 5,
  "score_threshold": 0.7,
  "rerank_enabled": true
}
```

**验收标准**：
- ✅ 配置文件已更新
- ✅ 所有参数已填写

**预计时间**：10 分钟

---

### 5.3 编写 API 调用测试脚本

**操作**：
创建一个 Python 脚本 `test_dify_api.py`，测试 Dify API 调用：

```python
import requests
import json
import sys

# 配置
DIFY_BASE_URL = "http://localhost:5001"  # 你的 Dify 地址
API_KEY = "YOUR_API_KEY_HERE"             # 你的 API Key
APP_ID = "YOUR_APP_ID"                    # 你的应用 ID

# API 端点
CHAT_MESSAGES_URL = f"{DIFY_BASE_URL}/v1/chat-messages"
RETRIEVE_URL = f"{DIFY_BASE_URL}/v1/datasets/YOUR_KNOWLEDGE_ID/retrieve"  # 知识库检索端点

# 请求头
headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "application/json"
}

def test_knowledge_retrieve(query):
    """测试知识库检索"""
    print(f"\n=== 测试知识库检索 ===")
    print(f"查询: {query}")
    
    payload = {
        "query": query,
        "top_k": 5,
        "score_threshold": 0.7,
        "rerank": True
    }
    
    try:
        response = requests.post(RETRIEVE_URL, headers=headers, json=payload, timeout=10)
        response.raise_for_status()
        result = response.json()
        
        print(f"检索成功！")
        print(f"返回片段数: {len(result.get('records', []))}")
        for i, record in enumerate(result.get('records', [])[:3]):  # 只显示前 3 条
            print(f"  片段 {i+1}:")
            print(f"    来源: {record.get('document', {}).get('name', '未知')}")
            print(f"    分数: {record.get('score', 0):.4f}")
            print(f"    内容: {record.get('content', '')[:100]}...")  # 只显示前 100 字符
        
        return result
    
    except requests.exceptions.RequestException as e:
        print(f"检索失败: {e}")
        return None

def test_chat_message(query):
    """测试对话消息"""
    print(f"\n=== 测试对话消息 ===")
    print(f"查询: {query}")
    
    payload = {
        "inputs": {},
        "query": query,
        "response_mode": "blocking",  # 非流式，等待完整响应
        "user": "test-user"
    }
    
    try:
        response = requests.post(CHAT_MESSAGES_URL, headers=headers, json=payload, timeout=60)
        response.raise_for_status()
        result = response.json()
        
        print(f"对话成功！")
        print(f"AI 回复: {result.get('answer', '')[:200]}...")  # 只显示前 200 字符
        
        return result
    
    except requests.exceptions.RequestException as e:
        print(f"对话失败: {e}")
        return None

def test_chat_message_streaming(query):
    """测试流式对话消息"""
    print(f"\n=== 测试流式对话消息 ===")
    print(f"查询: {query}")
    
    payload = {
        "inputs": {},
        "query": query,
        "response_mode": "streaming",  # 流式
        "user": "test-user"
    }
    
    try:
        response = requests.post(CHAT_MESSAGES_URL, headers=headers, json=payload, stream=True, timeout=60)
        response.raise_for_status()
        
        print(f"流式响应:")
        for line in response.iter_lines():
            if line:
                line = line.decode('utf-8')
                if line.startswith('data: '):
                    data = line[6:]  # 去掉 "data: " 前缀
                    try:
                        event = json.loads(data)
                        if event.get('event') == 'agent_message':
                            print(event.get('answer', ''), end='', flush=True)
                    except json.JSONDecodeError:
                        pass
        
        print(f"\n流式响应结束")
        return True
    
    except requests.exceptions.RequestException as e:
        print(f"流式对话失败: {e}")
        return False

if __name__ == "__main__":
    # 测试知识库检索
    test_knowledge_retrieve("深度学习图像识别专利")
    
    # 测试对话消息（非流式）
    test_chat_message("帮我写一份关于深度学习图像识别的专利交底书")
    
    # 测试流式对话（可选）
    # test_chat_message_streaming("帮我写一份关于深度学习图像识别的专利交底书")
    
    print("\n=== 测试完成 ===")
```

**验收标准**：
- ✅ 脚本能成功运行
- ✅ 知识库检索 API 调用成功
- ✅ 对话消息 API 调用成功（非流式和流式）

**预计时间**：30 分钟

---

### 5.4 运行测试脚本并验证

**操作**：
1. 在终端中运行测试脚本：
   ```bash
   python test_dify_api.py
   ```
2. 观察输出：
   - 知识库检索是否返回结果？
   - 对话消息是否返回生成的文档内容？
   - 引用标注是否出现？
3. 如果出错，检查：
   - API Key 是否正确？
   - Dify 服务是否可用？
   - 请求格式是否符合 Dify API 文档？

**验收标准**：
- ✅ 测试脚本运行成功
- ✅ 知识库检索 API 返回预期结果
- ✅ 对话消息 API 返回生成的文档内容
- ✅ 引用标注在返回内容中可见

**预计时间**：10 分钟

---

## 步骤 6：文档化配置参数（30 分钟）

### 目标
整理所有配置参数和测试结论，形成文档，供明天后端开发使用。

---

### 6.1 创建配置文档

**操作**：
创建一个文档 `DIFY_CONFIG.md`，记录以下信息：

```markdown
# Dify 配置文档

## 1. 服务信息

- **Dify 服务地址**: `http://localhost:5001`
- **API 版本**: `v1`

## 2. 知识库配置

- **知识库 ID**: `YOUR_KNOWLEDGE_ID`
- **知识库名称**: `PatentWriter-Test`
- **检索参数**:
  - Top-K: `5`
  - 相似度阈值: `0.7`
  - 重排序: `开启`

## 3. 应用配置

- **应用 ID**: `YOUR_APP_ID`
- **应用名称**: `PatentWriter-Agent`
- **应用类型**: `对话型应用`
- **API Key**: `YOUR_API_KEY_HERE`
- **API 端点**:
  - 对话消息: `POST /v1/chat-messages`
  - 知识库检索: `POST /v1/datasets/{dataset_id}/retrieve`

## 4. Prompt 模板

（粘贴你在步骤 2.2 中配置的 Prompt 模板）

## 5. 上下文变量

- `user_message`: 用户消息
- `references`: 知识库检索结果
- `history`: 对话历史

## 6. 模型配置

- **模型**: `gpt-3.5-turbo` (或你选择的模型)
- **温度**: `0.7`
- **最大生成长度**: `4096`

## 7. 测试结果

### 7.1 知识库检索测试

- ✅ 检索能返回结果
- ✅ 返回结果来自上传的文档
- ✅ 相似度分数 ≥ 0.7
- ✅ 检索响应时间 < 3 秒

### 7.2 对话生成测试

- ✅ AI 能生成专利文档内容
- ✅ 引用标注 `[①][②]` 出现
- ✅ 引用来源列表正确
- ✅ 多轮对话能保持上下文

### 7.3 API 调用测试

- ✅ 知识库检索 API 调用成功
- ✅ 对话消息 API 调用成功（非流式）
- ✅ 流式对话 API 调用成功

## 8. 问题与优化

（记录测试中遇到的问题和解决方案）

## 9. 下一步

（记录明天后端开发需要做的事情）
```

**验收标准**：
- ✅ 配置文档已创建
- ✅ 所有配置参数已记录
- ✅ 测试结果已记录
- ✅ 问题和优化建议已记录

**预计时间**：20 分钟

---

### 6.2 提交代码和文档

**操作**：
1. 将测试脚本 `test_dify_api.py` 和配置文档 `DIFY_CONFIG.md` 提交到你的代码仓库
2. 确保 `.gitignore` 中排除了配置文件中的敏感信息（如 API Key）
3. 写一个简短的 Git commit 消息：
   ```
   feat: 完成 Dify 对话式文档生成 Agent 配置和测试
   
   - 创建知识库并上传测试文档
   - 配置对话型应用和 Prompt 模板
   - 测试知识库检索和对话生成
   - 编写 API 调用测试脚本
   - 文档化配置参数
   ```

**验收标准**：
- ✅ 代码和文档已提交到仓库
- ✅ 敏感信息已排除
- ✅ Commit 消息清晰

**预计时间**：10 分钟

---

## 总结

完成以上 6 个步骤后，你将拥有：

1. **可工作的 Dify Agent**：能根据用户指令和知识库内容生成专利文档
2. **API 调用测试脚本**：能验证 Dify API 的正确性
3. **配置文档**：记录所有配置参数，供明天后端开发使用

**明天的工作**（后端开发）：
- 实现 `app/clients/dify_client.py`（调用 Dify API）
- 实现 `app/api/conversations.py`（对话管理和消息发送）
- 实现 `app/services/llm_svc.py`（LLM 调用封装）
- 实现 `app/core/citation_parser.py`（引用解析引擎）

---

## 三重身份视角审视

### 🔵 产品经理视角

| 关注点 | 评估 | 建议 |
|--------|------|------|
| **MVP 功能边界** | ✅ 今天专注 Dify 配置，明天专注后端开发，边界清晰 | 严格执行，不蔓延 |
| **引用标注价值** | ✅ 今天已验证引用标注格式，明天需确保解析正确 | 明天重点测试 CitationParser |
| **用户体验** | ⚠️ Dify UI 测试通过，但后端集成后需再次测试 | 明天集成测试时注意前端渲染 |

### 🟡 测试视角

| 关注点 | 评估 | 建议 |
|--------|------|------|
| **引用准确性** | ⚠️ 今天测试了引用标注格式，但未验证引用准确性（是否张冠李戴） | 明天增加引用准确性验证测试用例 |
| **Dify 不可用场景** | ⚠️ 今天未测试 Dify 服务不可用的情况 | 明天测试降级策略 |
| **API 调用边界** | ✅ 今天编写了 API 测试脚本，覆盖了正常场景 | 明天增加异常场景测试（超时、错误响应等） |

### 🟢 开发视角

| 关注点 | 评估 | 建议 |
|--------|------|------|
| **技术栈成熟度** | ✅ Dify 是成熟平台，API 文档完善 | 明天开发时参考 Dify 官方 API 文档 |
| **Dify 耦合度** | ✅ 通过 Client 层解耦，切换成本低 | 确保 `dify_client.py` 接口设计稳定 |
| **Prompt 工程** | ⚠️ 今天迭代了 Prompt，但可能还需要优化 | 明天预留 Prompt 调试时间 |

---

## 附加建议

1. **今天结束后**：
   - 确保 Dify 服务保持运行（如果是在本地）
   - 将 API Key 保存在安全的地方（不要提交到代码仓库）
   - 休息一下，明天专注后端开发

2. **明天开始前**：
   - 回顾今天的配置文档 `DIFY_CONFIG.md`
   - 确保 Dify 服务可用
   - 运行测试脚本 `test_dify_api.py` 验证 API 仍然可用

3. **如果遇到问题**：
   - Dify 官方文档：https://docs.dify.ai
   - Dify GitHub：https://github.com/langgenius/dify
   - 检查 Dify 服务日志（如果有错误）

---

**祝你今天工作顺利！有任何问题随时找我。**
